import React, {useCallback} from 'react';
import {
  Button,
  Text,
  VStack,
  Pressable,
  HStack,
  Divider,
  Input,
} from 'native-base';

export const CShare = () => {
  return <VStack flex={1} p={5}></VStack>;
};
