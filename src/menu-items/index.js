import top from './top';
import account from './account';
import subscribe from './subscribe';
import support from './support';

const menuItems = {
	items: [top, account, subscribe, support],
};

export default menuItems;
