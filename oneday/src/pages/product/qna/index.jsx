import { Container } from "./styles";
import React, { useCallback, useEffect, useState } from "react";
import { Link, Redirect, Route, Switch } from "react-router-dom";

import axios from "axios";

const Qna = ({ match }) => {
  return <Container>qna</Container>;
};

export default Qna;
